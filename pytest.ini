[pytest]
# server/ al sys.path -> "import myredis" funciona al correr pytest desde la raíz
pythonpath = server
testpaths = server/tests
# para los tests async (integración); silencia el warning de pytest-asyncio
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
